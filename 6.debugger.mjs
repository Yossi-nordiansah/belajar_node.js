function sayHello(name) {
    debugger;
    return `hello ${name}`;
}


const name = "Eko";
debugger;
console.log(sayHello(name));

//gunakan perintah node inspect <nama file> untuk masuk mode debugger 
//gunakan perintah c untuk continue
//gunakan perintah .exit untuk keluar dari mode debugger
//gunakan perintah watch("nama variable") untuk cek variable